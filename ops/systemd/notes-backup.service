# /etc/systemd/system/notes-backup.service 로 배치.
# notes-backup.timer 가 트리거하는 oneshot 백업 작업.
[Unit]
Description=Notes DB backup (pg_dump)
After=postgresql.service
Wants=postgresql.service

[Service]
Type=oneshot
User=app
Group=app
ExecStart=/srv/notes/backup.sh
