# /etc/grafana/provisioning/dashboards/notes.yml
# 아래 경로의 대시보드 JSON 들을 자동 로드.
apiVersion: 1
providers:
  - name: notes
    type: file
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
