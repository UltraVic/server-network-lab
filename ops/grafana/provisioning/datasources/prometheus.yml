# /etc/grafana/provisioning/datasources/prometheus.yml
# Prometheus 데이터소스를 선언적으로 등록(클릭 설정 불필요).
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
