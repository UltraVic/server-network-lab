# 서버 / 네트워크 학습 로드맵

> **목표**: 한 요청이 "브라우저 → 프록시 → 백엔드 → DB"를 거치는 길을 **직접 깔아보며** 서버·네트워크 구조를 이해한다.
> **방식**: Atomytics 프로젝트에서 본 *패턴*만 학습 대상으로 삼고(회사 코드·데이터는 안 가져옴), 무관한 토이 앱으로 재현한다.
> **토이 도메인**: 개인 메모/북마크 API (도메인은 중요치 않음 — 네트워크 배관이 주인공).

각 단계 포맷: **개념 → 직접 해보기 → 확인 방법 → Atomytics에선(대응 패턴)**.

---

## 추천 스택

| 레이어 | 선택 | 한 줄 이유 |
|---|---|---|
| 백엔드 | **FastAPI** (Python) | Atomytics와 동일 → 개념 전이 빠름, 마법 적음 |
| 프론트 | **Vite + 순수 JS** 1페이지 | 얇게 — 초점은 서버/네트워크 |
| 리버스 프록시 | **Nginx** (명시적) | 프록시 레이어를 *눈에 보이게* (Next rewrites가 숨기는 것을 직접) |
| DB | **SQLite → PostgreSQL** | 시작은 가볍게, 풀 학습 때 Postgres |
| 오케스트레이션 | **Docker Compose** | 포트 노출/내부망을 YAML로 가시화 (최고의 교보재) |
| 배포(심화) | **VPS + systemd + Caddy** | systemd/Bastion 토폴로지 축소 재현 |

> 도구는 목표(이해)를 위한 수단. 막히면 스택을 바꾸기보다 **개념**으로 돌아온다.

---

## 목표 아키텍처 (최종 형태)

```
                         ┌───────────────── Docker Compose network ─────────────────┐
 [브라우저] ──443──▶ [Nginx 리버스 프록시] ──/api──▶ [FastAPI :8000] ──▶ [Postgres :5432]
                    (호스트에 노출: 80/443)        (내부망 전용,            (내부망 전용,
                                                    호스트 비노출)          호스트 비노출)
                         └──────────────────────────────────────────────────────────┘
   + JWT 인증 · SSE 진행률 1화면 · .env 분리 · 무중단 재시작
```

---

## Phase 0 — 가장 단순한 한 줄 (직결)
**개념**: 클라이언트가 서버를 직접 호출하는 최소 형태. HTTP 요청/응답·포트·`localhost`의 의미.

**직접 해보기**
- FastAPI 앱 1개(`GET /notes`, `POST /notes`)를 `:8000`에 띄운다.
- 브라우저/`curl`로 `http://localhost:8000/notes` 호출.

**확인 방법**: `curl -v`로 요청 라인·헤더·상태코드를 눈으로 본다. 포트를 8000→8001로 바꿔 "포트 = 프로세스 주소"임을 체감.

**Atomytics에선**: 백엔드가 8000에서 `uvicorn`으로 뜨는 것과 동일. 단 거긴 8000을 외부에 안 연다 → Phase 2에서 그 이유를 만든다.

---

## Phase 1 — 프론트 분리 + CORS 깨뜨려보기
**개념**: 출처(Origin = scheme+host+port)와 **CORS**. 왜 `localhost:5173`(프론트)에서 `localhost:8000`(백엔드)을 직접 부르면 막히는가.

**직접 해보기**
- Vite 프론트(`:5173`)에서 `fetch('http://localhost:8000/notes')` 호출 → **CORS 에러를 일부러 본다.**
- 백엔드에 CORS 허용(`CORSMiddleware`)을 켰다 껐다 하며 차이 관찰.

**확인 방법**: 브라우저 콘솔의 CORS 차단 메시지, 응답의 `Access-Control-Allow-Origin` 헤더 유무.

**Atomytics에선**: 프론트가 절대 URL(`http://host:8000`)을 안 쓰고 `/api` 상대경로만 쓰는 이유 = 다른 출처가 안 생겨 CORS가 필요 없어짐 → Phase 2.

---

## Phase 2 — 리버스 프록시 (Nginx) + 포트 비노출 ⭐
**개념**: 리버스 프록시. 프론트와 API를 **한 출처**로 묶어 CORS를 없애고, 백엔드 포트를 **외부에 안 여는** 보안 이점.

**직접 해보기**
- Nginx를 앞단(`:80`)에 두고: `/` → 프론트 정적파일, `/api/` → `proxy_pass http://backend:8000`.
- 프론트 fetch를 `/api/notes`(상대경로)로 바꾼다. → CORS 사라짐 확인.

**확인 방법**: 브라우저는 80만 본다. `:8000`을 직접 치면? (다음 단계에서 진짜로 막는다.)

**Atomytics에선**: Next `rewrites`가 정확히 이 역할(`/api/:path*` → 백엔드). 여기선 일부러 Nginx로 **명시적으로** 깔아 레이어를 본다.

---

## Phase 3 — Docker Compose로 네트워크 토폴로지 가시화 ⭐
**개념**: 컨테이너 네트워크. **호스트 노출(`ports:`) vs 내부망 전용**. "8000 비노출"의 실체.

**직접 해보기**
- 3개 서비스(`nginx`, `backend`, `db`)를 Compose로.
- `nginx`만 `ports: ["80:80"]`. `backend`·`db`는 `ports` 없이 **내부망**으로만 통신.
- 호스트에서 `curl localhost:8000` → **연결 거부**(노출 안 됨) 확인. 하지만 nginx→backend는 서비스명(`backend:8000`)으로 통신됨.

**확인 방법**: `docker compose ps`로 노출 포트 비교. `docker exec nginx curl backend:8000` 는 되고, 호스트 `curl localhost:8000` 은 안 됨.

**Atomytics에선**: 백엔드 8000을 외부에 안 노출 = 정확히 이 원리(거긴 systemd+사설망, 여긴 Compose 내부망).

---

## Phase 4 — 인증: JWT 한 바퀴
**개념**: 인증(누구냐) vs 인가(뭘 보냐). 토큰 발급 → 매 요청 첨부 → 서버 검증.

**직접 해보기**
- `POST /login`(아이디/비번 → JWT 발급), 보호 라우트 `GET /notes`는 `Authorization: Bearer` 검증.
- 프론트는 토큰 저장 후 fetch에 자동 첨부(작은 wrapper).
- 만료/위조 토큰 → 401 흐름 확인.

**확인 방법**: 토큰 없이 호출 → 401, 변조 토큰 → 401, 정상 → 200. (jwt.io로 페이로드 디코드해보기)

**Atomytics에선**: `fetchWithAuth`가 Bearer 자동 첨부, 백엔드 `get_current_user`/`require_approved`가 검증. (OAuth·RBAC는 심화로 미룸)

---

## Phase 5 — DB 커넥션 풀 + 비동기
**개념**: 커넥션은 비싸다 → **풀**로 재사용. 동기 드라이버를 어떻게 비동기 서버에서 안전히 쓰나.

**직접 해보기**
- SQLite → **Postgres**로 교체. 커넥션 풀(예: `asyncpg`/`psycopg_pool`) 적용.
- 풀 크기 1 vs 10으로 동시요청(`ab`/`hey`) 던져 응답시간 비교.

**확인 방법**: 부하 도구로 동시 50요청 → 풀 작을 때 지연·대기 관찰. 로그로 커넥션 재사용 확인.

**Atomytics에선**: 동기 `oracledb`를 `ThreadPoolExecutor`로 감싸 비동기화 + 풀 재사용([database.py] 패턴). 여기선 그 "왜"를 부하로 체감.

---

## Phase 6 — SSE (실시간 진행률)와 프록시 우회
**개념**: 스트리밍(SSE)은 일반 요청과 다르다 — 헤더 제약·프록시 버퍼링. 왜 별도 취급하나.

**직접 해보기**
- `GET /tasks/{id}/progress` 를 SSE(`text/event-stream`)로 진행률 0→100% 스트리밍.
- Nginx 기본 버퍼링 때문에 끊겨 보이는 현상 → `proxy_buffering off` 로 해결.
- SSE는 헤더 못 붙이니 토큰을 쿼리파라미터(짧은 수명)로 전달.

**확인 방법**: 진행률이 실시간으로 흐르는지. 버퍼링 켰을 때 vs 껐을 때 차이.

**Atomytics에선**: EventSource가 프록시 우회해 백엔드 직결 + 짧은 수명 stream 토큰 사용. 동일 함정·동일 해법.

---

## Phase 7 — 설정/시크릿 분리 + 배포(무중단)
**개념**: 코드와 설정 분리(`.env`), 시크릿 비커밋, 무중단 재시작, (심화) 사설망/점프호스트.

**직접 해보기**
- `.env`(DB비번·JWT키)를 코드 밖으로. `.gitignore` 등록. `.env.example`만 커밋.
- VPS 1대에 배포: Caddy(자동 HTTPS) + systemd로 백엔드 데몬화 + `systemctl restart` 무중단.
- (심화) DB 포트를 방화벽으로 막고, SSH 터널/점프호스트로만 접속.

**확인 방법**: 설정 바꾸고 재시작해야 반영되는 것, 시크릿이 git에 안 들어간 것, 재배포 중 다운타임 측정.

**Atomytics에선**: `.env` 배포 tar 제외·서버 보존, systemd 2서비스, Bastion 경유, OS env > .env 우선순위. 토폴로지 그대로 축소판.

---

## 학습 순서 요약 (점진적 — 각 단계가 이전 위에 쌓임)
```
0 직결 → 1 CORS 깨보기 → 2 프록시로 한출처 → 3 Compose 내부망(포트비노출)
       → 4 JWT → 5 커넥션풀+비동기 → 6 SSE → 7 시크릿분리+무중단배포
```
- **1·2·3** 이 서버/네트워크의 핵심(출처·프록시·포트). 여기까지만 해도 목표의 절반.
- 각 Phase 끝에 "**Atomytics에선**" 칸으로 회사 코드와 1:1 대조하면 이해가 굳는다.

## 다음 액션 후보
- 이 로드맵으로 **Phase 0~3 토이앱 스캐폴딩**(이 폴더에 실제 코드 생성)
- 특정 Phase의 **개념을 더 깊게** 설명 (예: CORS preflight, JWT 서명 검증 원리)
- 부하 테스트·Compose 등 **도구 사용법** 안내

> 참고 학습 소스: 회사 프로젝트의 `docs/ONBOARDING.md` §2(요청 흐름)·§4(배포·운영)가 이 로드맵과 1:1로 대응 — 패턴 이해용으로만 참고(코드 복제 금지).
